---
description: Preview augmentation on a single episode (saves before/after PNGs)
argument-hint: '[--dataset <repo_id>] [--episode <n>]'
---

Use the `robotq:preview` skill to preview augmentation effects on a single episode.

The user wants to preview an augmentation. Follow the instructions in the `robotq:preview` skill. Invoke it now.
