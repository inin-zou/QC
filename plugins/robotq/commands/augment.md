---
description: Augment a LeRobot v3 dataset with composable transforms
argument-hint: '[--dataset <repo_id> --output <repo_id>] [--config <yaml>]'
---

Use the `robotq:augment` skill to augment a LeRobot v3 robotics dataset.

The user wants to augment a dataset. Follow the instructions in the `robotq:augment` skill. Invoke it now.
