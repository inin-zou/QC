---
description: Generate a YAML pipeline config for RobotQ augmentation
argument-hint: ''
---

Use the `robotq:configure` skill to help the user create a YAML pipeline configuration file.

The user wants to configure an augmentation pipeline. Follow the instructions in the `robotq:configure` skill. Invoke it now.
